const generic200 = { type: 'string' };

const generic400 = { type: 'string' };

const generic403 = { type: 'string' };

const generic500 = { type: 'string' };

module.exports = {
  generic200,
  generic400,
  generic403,
  generic500,
};
